# Update these with your own project keys from the Supabase Dashboard
NEXT_PUBLIC_SUPABASE_URL=your-project-url-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

#Send the real URL and ANON_KEY to your teammates via a private message - Tell them to:

#Take .env.example and rename it to .env.local.

#Paste the keys you sent them into that file.

#Restart their server (npm run dev).?